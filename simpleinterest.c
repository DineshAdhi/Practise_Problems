#include<studio.h>
int main()
{
  int n; // No. of years
  float p, r; // Princple and rate of interest in float
  float simpleInterest; // Final answer from the calculation;
  printf("Enter the no.o years");
  scanf("%d", &n);

  printf("Enter the rate of interest and Principle");
  scanf("%f,%f", &r, &f);

  simpleInterest = (p*n*r)/100;

  printf("The simple interest is: %f", simpleInterest);
  
  return 0;

}
