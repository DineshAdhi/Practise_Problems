#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;

int main()
{


  // vector<int> a;
  // long long int i,n,hit,t; cin>>n>>hit>>t;
  //
  // for(i=0;i<n;i++)
  // {
  //   int s; cin>>s; a.push_back(s);
  // }
  //
  // sort(a.begin(), a.end());
  //
  // long long int maxhits = hit * t, count = 0;
  //
  // for(i=0;i<n;i++)
  // {
  //   if(a[i] > 0)
  //   {
  //   maxhits -=a[i];
  //   if(maxhits <= 0)
  //     break;
  //   else
  //     count++;
  //   }
  // }
  //
  // cout<<count;
}
